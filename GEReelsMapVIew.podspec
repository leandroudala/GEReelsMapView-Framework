Pod::Spec.new do |s|
    s.name         = "GEReelsMapView"
    s.version      = "1.0.0"
    s.source       = { :git => "https://github.com/leandroudala/GEReelsMapView.git", :branch => "main" }
    s.authors      = { "Gabriel Farias" => "gabriel.farias@radixeng.com", "Leandro Udala" => "leandro.correia@radixeng.com" }
    s.license      = { :type => "MIT" }
    s.summary      = "GEReelsMapView lib for Reels Management System iOS app"
    s.homepage     = "https://github.com/leandroudala/GEReelsMapView"
    s.source_files = "GEReelsMapView/**/*.{h,m}"
  end